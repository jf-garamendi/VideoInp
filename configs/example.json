{
  "general": {
    "exp_name": "Overfitting",
    "agent": "EncDec_update_agent_A",
    "mode" : "train",
    "log_dir": "./logs/",
    "device": "cuda"
  },

  "data": {
    "train_root_dir": "./dataset",
    "val_root_dir": "./dataset",
    "train_batch_size": 1,
    "val_batch_size": 1,
    "number_of_frames": 10,
    "random_holes_on_the_fly": true,
    "n_random_holes_per_frame": 5
  },

  "optimizer": {
    "weight_decay": 4e-5,
    "learning_rate": 1e-4,
    "beta_1" : 0.9,
    "beta_2" : 0.999
  },

  "model": {
    "max_num_steps_update": 20,
    "encDec" : "EncDec_pierrick",
    "update" : "Update_pierrick",
    "partial_mode_update" : "pow"
  },

  "training": {
    "encDec_n_epochs": 5,
    "update_n_epochs": 5
  },

  "verbose": {
    "tensorboard_root_dir": "./verbose/tensor_board/",
    "verbose_out_images" : "./verbose/training_out/"
  },

  "checkpoint": {
    "saving_n_iter": 1,
    "root_dir": "./checkpoint/",
    "enc_dec_checkpoint_filename" : "enc_dec_checkpoint.tar",
    "update_checkpoint_filename": "update_checkpoint.tar"

  },

  "losses": {
    "encDec_losses": {
      "losses": ["L1_loss"],
      "weights": [1]
    },
    "update_losses": {
      "losses": ["L1_loss", "TV_loss", "Min_fbbf_loss"],
      "weights": [1, 10, 2]
    }
  },

  "custom_layers": {
    "partial_mode_update": "pow"
  }
}